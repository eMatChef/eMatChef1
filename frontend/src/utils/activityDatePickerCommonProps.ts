/** Gemeinsame VDatePicker-Props — Steuerleiste via #controls (ActivityDatePickerControlsBar). */
export const activityDatePickerCommonProps = {
  color: 'primary' as const,
  hideHeader: true,
  showAdjacentMonths: true,
}
